// Carry Engine Configuration for Simulator
// Include this in your Debug.xcconfig

// Path to the Carry engine (3 levels up from ios/ to reach /Volumes/ssd/carry/engine)
CARRY_ENGINE_PATH = $(SRCROOT)/../../../engine

// Header search path
HEADER_SEARCH_PATHS = $(inherited) $(CARRY_ENGINE_PATH)/include

// Force load the static library to prevent symbol stripping (simulator version)
OTHER_LDFLAGS = $(inherited) -force_load $(CARRY_ENGINE_PATH)/target/ios/CarryEngine.xcframework/ios-arm64_x86_64-simulator/libcarry_engine.a
